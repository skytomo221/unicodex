<div class="relative
            z-1
            <%= @type == :main ? "w-60 h-60" : "w-20 h-20" %>
            rounded-xl
            border border-gray-300
            flex items-center justify-center
            <%= @type == :main ? "text-9xl" : "text-3xl" %>
            <%= @type == :main ? "text-gray-950" : "text-gray-500" %>
            bg-[#fafafa]
            shadow-md">
  <%= render(Glyph.new(glyph: @unicode_character.glyph, font: :sans)) %>
</div>
